// Crie uma classe "Circle" (Círculo) com um atributo para o raio e métodos para calcular a área e o comprimento do círculo.
public class App {
    public static void main(String[] args) throws Exception {
       Circulo circulo = new Circulo(5);
        circulo.areaCirculo();
       
    }
}
